import 'package:flutter/material.dart';

/// Buddyplan brandbook design tokens (light mode primary).
class BuddyplanColors {
  BuddyplanColors._();

  static const teal = Color(0xFF2C6A6B);
  static const darkTeal = Color(0xFF1F4A4B);
  static const coral = Color(0xFFD8745E);
  static const background = Color(0xFFF7FAFC);
  static const card = Color(0xFFFFFFFF);
  static const slateDark = Color(0xFF2D3748);
  static const mutedSlate = Color(0xFF718096);
  static const mutedGray = Color(0xFFE2E8F0);
  static const deepCharcoal = Color(0xFF1A202C);
  static const paleGray = Color(0xFFEDF2F7);
  static const borderDark = Color(0xFF4A5568);

  /// 40% teal on white — completed / placeholder state (brandbook §1).
  static const tealStructure40 = Color(0x99759B9C);

  static const borderRadius = 12.0;
}
